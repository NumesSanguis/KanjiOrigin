#:kivy 1.9.1
#:import dbmanager data.screens.dbmanager

<BoxLayoutS@BoxLayout>:
    spacing: '14dp'

KanjiOriginScreen:
    name: 'DBmanager'
    id: dbms
    fullscreen: True
    has_screenmanager: True
    on_pre_enter: app.ac_prev.on_release = app.screen_changer()

    DatabaseManager:
        id: dbm
        on_current_screen:
            idx = self.screen_names.index(args[1].name)